% This function accepts a model and returns a struct where element names
% are the names of the markers and the positions are included as SimTK vec3
% objects

% Copyright RCNL *change later*

% (Model) -> (struct)
% make struct of marker positions, organized by name
function markerPositions = findMarkerModelPositions(model)

end

