% Wrapper function to compute the inverse kinematics of a system using the
% OpenSim methodology. This wrapper allows for easy calling of IK based on
% simple parameter arguments.

% Copyright RCNL *change later*

% (Model, params) -> (struct)
% Compute a motion through inverse kinematics based on input params
function motion = computeInverseKinematics(model, params)

end

