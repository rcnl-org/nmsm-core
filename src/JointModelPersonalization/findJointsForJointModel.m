% This function processes the optimization plus the inputs and params of
% Joint Model Personalization to produce the joint array.

%Copyright RCNL *change later*

% (struct, struct, struct) -> (Array of Joints)
% Retrieves the joint array from the inputs and parameters
function joints = findJointsForJointModel(optimization, inputs, params)

end

