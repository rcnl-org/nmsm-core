% This function takes the input values and returns the parameters for the
% IK algorithm to be used in the kinematic calibration.

% Copyright RCNL *change later*

% (Model, struct) -> (struct)
% Using the input values, the params for the IK algorithm are prepared
function output = prepareInnerOptimizationParams(model, params)

end

