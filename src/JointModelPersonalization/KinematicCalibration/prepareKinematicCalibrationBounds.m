function [lowerBounds, upperBounds] = prepareKinematicCalibrationBounds(...
    functions, params)
lowerBounds = valuesOrAlternate(params, 'lowerBounds', ...
    zeros(max(size(functions)),1));
upperBounds = valuesOrAlternate(params, 'upperBounds', ...
    zeros(max(size(functions)),1));
end