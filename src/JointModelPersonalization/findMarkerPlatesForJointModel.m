% This function processes the optimization plus the inputs and params of
% Joint Model Personalization to produce the marker plate array.

%Copyright RCNL *change later*

% (struct, struct, struct) -> (Array of MarkerPlates)
% Retrieves the joint array from the inputs and parameters
function markerPlates = findMarkerPlatesForJointModel(optimization, ...
    inputs, params)

end

